import { timelineRequest } from '@/app/api-routes/stories/timeline-routes';
export const GET = (request: Request) => timelineRequest(request);
export const POST = (request: Request) => timelineRequest(request);
