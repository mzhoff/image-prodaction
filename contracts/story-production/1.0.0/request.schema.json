{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "storyId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160,
      "description": "Stable story identifier supplied by the publishing system."
    },
    "revisionId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160,
      "description": "Immutable story revision identifier."
    },
    "slideId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 160,
      "description": "Stable identifier of the slide whose background is being produced."
    },
    "brief": {
      "type": "string",
      "minLength": 1,
      "maxLength": 8000,
      "description": "Plain-language visual brief for a single background image."
    },
    "altText": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional accessibility description and semantic guardrail."
    },
    "locale": {
      "type": "string",
      "minLength": 2,
      "maxLength": 35,
      "description": "Content locale used while interpreting the brief."
    },
    "aspectRatio": {
      "type": "string",
      "enum": ["9:16"],
      "description": "Stories pilot aspect ratio."
    },
    "imageSize": {
      "type": "string",
      "enum": ["1K"],
      "description": "Pinned provider generation size for the first pilot."
    },
    "formatKey": {
      "type": "string",
      "enum": ["story-full-hd"],
      "description": "Composition target format; the preview compositor owns exact pixels."
    }
  },
  "required": [
    "storyId",
    "revisionId",
    "slideId",
    "brief",
    "locale",
    "aspectRatio",
    "imageSize",
    "formatKey"
  ]
}
