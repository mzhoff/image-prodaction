# REVERIE assistant pilot: new canonical Workspace projection

Owner authorized a new REVERIE Content Hub account linked to existing Image
Production `mzh@ya.ru`, and a public consultant pilot on 2026-09-11.

Create a new empty TEAM projection with the Content Hub canonical Workspace ID
`a54cdbe5-1799-473f-9780-6e0e53786f84`. Existing IP user
`PIINPnqwnW7k8nKwbK3HSIrcxgGLc8oY` owns this projection. No personal Workspace is
renamed, merged or reassigned. No projects, pipelines, provider keys or paid
runtime grants are copied. Explicit identity/projection links live in ChatModule
assistant-host's registry; they are not shared login or authorization bypasses.

`scripts/provision-reverie-workspace.ts` is dry-run by default, refuses conflicts,
and creates only the new Workspace and owner membership when passed `--apply`.
