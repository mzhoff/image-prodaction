# Legacy Pages Router placeholder

This directory is intentionally kept at the repository root so Next.js does not treat `src/pages` as the legacy Pages Router.

The application uses the App Router in root `app/`. Feature-Sliced Design page slices live in `src/pages/`.
