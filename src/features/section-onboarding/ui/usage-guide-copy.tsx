export function UsageAccountingDetails({ tUi }: { tUi: (source: string) => string }) {
  return <details className="section-guide-details"><summary>{tUi("Что входит в расчёт")}</summary>
    <p>{tUi("Источник — журналы генераций Production и вызовов ассистента выбранного рабочего пространства. Один запрос — один физический вызов модели. Повторная отправка считается отдельно, опрос статуса и уточнение стоимости — нет. Очередь до отправки провайдеру не считается запросом.")}</p>
    <p>{tUi("Запрос попадает в день первого наблюдения вызова, результат — в день завершения. Результаты — успешные генерации и редактирование изображений, Generate Text и форматирование, аудио и видео. Анализ, транскрибация, ответы чата, импорт и сохранённые копии не увеличивают счётчик результатов.")}</p>
    <p>{tUi("Расходы включают известную стоимость успешных и неудачных вызовов. Это не остаток баланса. «≥» — известная часть суммы, «—» — данных нет. Нулевые суммы старого журнала чата считаются неизвестными. Токены не заменяют стоимость видео и аудио.")}</p>
    <p>{tUi("Вызовы без записей журнала нельзя восстановить точно. Кэш и reasoning отдельно не показаны: для них пока нет единых достоверных полей.")}</p>
  </details>;
}

export function UsageComparisonDetails({ tUi }: { tUi: (source: string) => string }) {
  return <details className="section-guide-details"><summary>{tUi("Как сравниваются периоды")}</summary>
    <p>{tUi("Сравниваем с непосредственно предшествующим периодом той же длины. Если текущий ещё не закончился, берём одинаковые по длительности части. Модель и тип операции применяются к обоим периодам.")}</p>
    <p>{tUi("Изменение = (текущее − предыдущее) / предыдущее × 100%. «Ранее 0» — рост с нулевой базы; «Нет сравнения» — недостаточно данных. Рост расходов сам по себе не означает улучшения.")}</p>
  </details>;
}
