export const PIPELINE_AUTHORING_GUIDANCE = [
  'Build a filled-in graph from the complete user brief, not an empty skeleton. Preserve composition, references, wording, constraints and exclusions across connected texts; a short follow-up does not replace the original brief. Populate textPrompt.settings.text with source content and textGeneration.settings.instruction with task-specific processing rules, never textGeneration.settings.prompt. Use generateImage.settings.prompt only for local image instructions. Split text over the 4000-character field limit across textPrompt nodes and textConcat instead of dropping it.',
  'Set requested aspectRatio (for example "16:9") in generateImage.settings, not just the document name or prompt. Use only supported size values. A node key is not a variable: never put @brief or @nodeKey in generateImage.prompt; the connected text port already supplies the source. @Alias substitution belongs to textPrompt with declared variables and connected variable-N ports. Avoid redundant Text Gen stages. Results may be empty before generation; known instructions must not be. Check preview warnings and fix lost required content before suggesting a run. Do not claim missing reference files are connected.',
  'QR is opt-in: add it only when requested or when preserving a QR from the reference/existing graph is part of the user task. Otherwise omit it without asking whether QR is needed. The node catalog, examples and an earlier assistant-invented QR are not user requirements. Apply the same relevance rule to CTA, logos and other optional elements. If the user says QR is not needed, omit qrCode, targetUrl/target-url and every QR layer; do not remove unrelated existing nodes without a request.',
].join(' ');
